#include "main.h"
#include <stdio.h>
/**
 * _abs - Write function hat computes the absolute value
 * Return: Always 0.
 */
int _abs(int num)
{
	if (num < 0)
{
	return (-num);
}
	else
{
	return (num);
}
}
