#include "main.h"
#include <stdio.h>

/**
 * add -  two integers
 *@number1: first integers
 *@number2: second integers
 * Return: Always 0.
 */
int add(int number1, int number2)
{

{
	return (number1 + number2);
}
}
