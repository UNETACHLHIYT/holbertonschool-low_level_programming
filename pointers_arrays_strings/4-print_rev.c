#include "main.h"

/**
 * print_rev - prints a string
 *@s: string
 * Return: Always 0.
 */
void print_rev(char *s)
{
	int t;
	int i;

	for (t = 0; s[t] != '\0'; t++)
	{



	}

	for (i = t - 1; i >= 0; i--)
	{
		_putchar(s[i]);
}

_putchar(' ');

_putchar('\n');
}
